# Secrets & credentials
.env
credentials.json
token.json
.pacos_space_token

# Generated per-lead assets and state
output/
tracker.csv
new_jobs.csv

# Real lead data (user-supplied; never commit)
data/leads.csv
data/leads_raw.csv
data/leads_enriched.csv
data/base_cv.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# PyInstaller exe build (re-mint with build_exe.bat; see docs/adr/0004)
dist/
build/
*.spec
PACOS.exe

# SQLite tracker (source of truth, per-operator)
tracker.db
tracker.db-shm
tracker.db-wal

# Built frontend (bundled into the exe; rebuilt by build_exe.bat)
frontend/dist/
frontend/node_modules/
